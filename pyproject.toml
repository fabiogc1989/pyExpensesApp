[project]
name = "pyexpensesapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
    "sqlalchemy>=2.0.45",
    "tkcalendar>=1.6.1",
]

[tool.uv]
package = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
my-app = "pyexpensesapp.main:main"